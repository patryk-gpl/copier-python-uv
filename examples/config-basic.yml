# Example data file for non-interactive Copier usage
# Provide to Copier with --data-file to pre-answer questions (do NOT use -a here)
# Usage:
#   copier copy --data-file examples/config-basic.yml /path/to/template /dest/path
# Only include question keys (no leading underscore settings). You can override
# any value inline with -d key=value flags if needed.

project_name: sample_project
project_description: "Sample project generated via Copier non-interactively"
author_name: Test User
author_email: test@test.com
production_deps: ["httpx"]
# Include both pytest & ruff to show conditional README sections
dev_deps: ["pytest", "pytest-cov", "ruff"]
